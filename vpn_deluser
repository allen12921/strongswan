#!/bin/sh

vpn_user=$1

if [ -z ${vpn_user} ]; then
	echo "Usage: $0 user"
	exit 1
fi

touch /etc/ipsec.secrets
touch /etc/l2tp-secrets

sed -i "/${vpn_user} :/d" /etc/ipsec.secrets
sed -i "/\"${vpn_user}\" \"*\"/d" /etc/l2tp-secrets
